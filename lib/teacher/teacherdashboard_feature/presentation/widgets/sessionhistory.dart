class SessionHistory {
  final String className, classTitle, date, time;
  final int totalStudents, presentStudents;

  SessionHistory({
    required this.className,
    required this.classTitle,
    required this.date,
    required this.time,
    required this.totalStudents,
    required this.presentStudents,
  });
}

import 'package:flutter/material.dart';


class AppColors {
  // Background Gradients
  static const Color backgroundStart = Color(0xFF059669); // emerald-600
  static const Color backgroundVia = Color(0xFF10B981);   // emerald-500
  static const Color backgroundEnd = Color(0xFF0D9488);   // teal-600

  // Text Colors
  static const Color textWhite = Colors.white;            // #ffffff
  static const Color textWhite90 = Color.fromRGBO(255, 255, 255, 0.9);
  static const Color textWhite70 = Color.fromRGBO(255, 255, 255, 0.7);

  // Sparkles
  static const Color sparkle = Color(0xFFFCD34D); // yellow-300

  // Cards / Glow
  static const Color glowCircle = Color.fromRGBO(255, 255, 255, 0.2);
  static const Color glowBorder = Color.fromRGBO(255, 255, 255, 0.4);

  // Islamic Theme Colors
  static const Color islamicGreen = Color(0xFF059669);
  static const Color islamicEmerald = Color(0xFF10B981);
  static const Color islamicNavy = Color(0xFF1E3A8A);
  static const Color islamicGold = Color(0xFFD4AF37);
  static const Color islamicLightGreen = Color(0xFFE0F2E9);


  // Secondary / Accent
  static const Color primary = Color(0xFF059669);
  static const Color primaryForeground = Colors.white;
  static const Color secondary = Color(0xFF10B981);
  static const Color secondaryForeground = Colors.white;
  static const Color accent = Color(0xFFD4AF37);
  static const Color accentForeground = Color(0xFF1A2B3C);
  static const Color destructive = Color(0xFFDC2626);
  static const Color destructiveForeground = Colors.white;

  // Input / Border
  static const Color border = Color.fromRGBO(5, 150, 105, 0.1);
  static const Color inputBackground = Color(0xFFF3F4F6);
  static const Color switchBackground = Color(0xFFCBD5E1);


  // 50 (Very Light)
static const Color islamicNavy50 = Color(0xFFEFF6FF);

// 100
static const Color islamicNavy100 = Color(0xFFDBEAFE);

// 200
static const Color islamicNavy200 = Color(0xFFBFDBFE);

// 300
static const Color islamicNavy300 = Color(0xFF93C5FD);

// 400
static const Color islamicNavy400 = Color(0xFF60A5FA);

// 500 (Base Blue)
static const Color islamicNavy500 = Color(0xFF3B82F6);

// 600
static const Color islamicNavy600 = Color(0xFF2563EB);

// 700
static const Color islamicNavy700 = Color(0xFF1D4ED8);

// 800
static const Color islamicNavy800 = Color(0xFF1E40AF);

// 900 (Your Color – Deep Navy)
static const Color islamicNavy900 = Color(0xFF1E3A8A);

// 950 (Extra Dark)
static const Color islamicNavy950 = Color(0xFF172554);


// Pink 500
static const Color pink500 = Color(0xFFEC4899);

// Pink 700
static const Color pink700 = Color(0xFFBE185D);

}
